// Proxy to packages/msp/src/usage/cli/gks-cli.ts
// In development (src), we go up 2 levels. In production (dist), we go up 3 levels.
// Using a dynamic or more robust pathing is better, but for now we fix the relative path.
import '../../../packages/msp/src/usage/cli/gks-cli.js';
