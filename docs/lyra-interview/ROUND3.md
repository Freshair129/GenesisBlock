# ROUND3 - LYRA

## L-R3.1
**Verdict:** [derived] A 12B role-played self-report should carry low evidentiary weight: useful for hypothesis generation, not for product truth, buyer truth, or architecture truth. [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:8]

**Evidence:** [asserted] The Round 3 summary itself labels the method as two local 12B models queried via Ollama in role-play personas and says the reports are "hypotheses", "possibly anthropomorphized", and "NOT ground truth." [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:8] [asserted] The worker model says it wants three simple tools, no query language, no in-engine fusion, and only debug/audit bitemporal value. [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:12] [asserted] The orchestrator model says VRAM scarcity flips fusion/ranking and versioned state toward the database. [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:21] [derived] Those statements are directional prompts for falsification only, because LYRA requires every number and claim to be cited or marked unknown, and forbids plausible inference as evidence. [docs/lyra-interview/LYRA.md:51] [unknown] No trace here shows the models performing real tasks with alternative storage designs, so "worker does not want fusion" versus "orchestrator does" is not yet a measured behavioral signal. [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:8]

**Falsifier / what would settle it:** [derived] The real experiment is an A/B/C task harness, not a model opinion: run identical multi-agent workloads under constrained and unconstrained VRAM with (A) app-side fusion over vector/KV/keyword tools, (B) GenesisBlock HQL/G3 fusion, and (C) SQLite/Redis baseline; measure task success, context tokens spent merging, round trips, p50/p99 latency, stale-read/lost-update incidents, and debug time. [docs/BENCH-SPEC--HQL-MOAT-AND-EXPRESSIVENESS.md:52] [derived] The scarcity-flips-fusion claim is confirmed only if DB-side fusion materially improves constrained-orchestrator outcomes while not being required in unconstrained worker conditions. [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:27] [derived] It is refuted if the worker/orchestrator performance gap disappears, or if HQL/G3 fails the existing kill threshold: less than 20% p50 savings and no >=2x round-trip reduction on the target query set. [docs/BENCH-SPEC--HQL-MOAT-AND-EXPRESSIVENESS.md:85]

**Open questions:** [unknown] What exact local-agent tasks, VRAM ceilings, model sizes, and success thresholds should define "scarcity" for the benchmark? [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:21] [unknown] Should the benchmark score end-task quality, operator debug time, or only storage/query metrics? [docs/BENCH-SPEC--HQL-MOAT-AND-EXPRESSIVENESS.md:27]

## L-R3.2
**Verdict:** [derived] "Buyer = orchestrator + on-device flagship" is a sharper hypothesis than "buyer = bare worker", but it is not proven; it is falsified if orchestrators and phones solve the same jobs with simpler shared state plus sync. [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:46]

**Evidence:** [asserted] The Round 3 derived conclusion claims scarcity plus concurrency makes the buyer the orchestrator and on-device flagship, not the bare worker. [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:46] [measured] GoVibe evidence shows an orchestrator-shaped retrieval path already exists through multi-hop search and RRF-style composition, which is a relevant target for a "one query beats app composition" test. [docs/genesis-interview/evidence/ground-govibe.md:90] [asserted] The on-device block claims a phone-local database should keep operational state/privacy/offline continuity while cloud sees curated summaries, and that CRDT/LWW sync is a fit. [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:32] [measured] The crate does have a mobile feature and FFI feature for iOS/Android builds. [Cargo.toml:34] [measured] The current C FFI exposes open, add node, search, execute HQL, retrieve context, flush index, close, and free string functions; it does not expose `events_since` or `reconcile_state`. [src/ffi.rs:107] [src/ffi.rs:157] [src/ffi.rs:191] [src/ffi.rs:224] [src/ffi.rs:255] [src/ffi.rs:289] [src/ffi.rs:139] [src/ffi.rs:307] [derived] Therefore, phone-cloud sync readiness is not established by the current FFI surface. [src/ffi.rs:1] [measured] The mobile spec still leaves physical iOS launch, physical Android launch, and CRDT sync exchange with desktop unchecked. [docs/SPEC--MOBILE-SDK.md:355] [docs/SPEC--MOBILE-SDK.md:356] [docs/SPEC--MOBILE-SDK.md:361] [derived] SQLite remains a plausible null hypothesis because local evidence says SQLite WAL gives real single-snapshot consistency and can combine sqlite-vec with SQL/CTEs in one file. [docs/genesis-interview/evidence/r2-sqlite.md:56] [docs/genesis-interview/evidence/r2-sqlite.md:60]

**Falsifier / what would settle it:** [derived] The buyer claim is wrong if orchestrator pilots use Redis, SQLite WAL, files, or app-side RRF without adopting graph+vector+bitemporal fusion. [docs/genesis-interview/evidence/r2-sqlite.md:60] [derived] It is also wrong if phones use SQLite plus a normal sync SDK and do not need GenesisBlock's graph, G3, bitemporal, or CRDT layer. [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:81] [derived] It is strengthened only by a buyer-facing pilot where GenesisBlock beats the app-side and SQLite/Redis baselines on the same orchestrator or phone-cloud tasks, with the HQL/G3 benchmark gates and mobile sync DoD satisfied. [docs/BENCH-SPEC--HQL-MOAT-AND-EXPRESSIVENESS.md:85] [docs/SPEC--MOBILE-SDK.md:361]

**Open questions:** [unknown] Which concrete orchestrator product, buyer, or integration has committed to graph+vector+bitemporal instead of Redis/SQLite? [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:81] [unknown] What phone-cloud app proves the local database must sync conflict-free without exposing raw DB state to the cloud? [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:32]

## L-R3.3
**Verdict:** [derived] GenesisBlock currently shows serialized writes plus WAL durability inside a process, not proven MVCC snapshot isolation for concurrent multi-agent state; the "single-writer-vs-snapshot" tension is real. [src/router.rs:26] [src/lib.rs:2035]

**Evidence:** [measured] The REST app state is `Arc<RwLock<Storage>>`, so REST handlers share one storage object behind a global read/write lock. [src/router.rs:26] [measured] Mutating REST routes acquire `state.storage.write()` for node, edge, collection, rebuild, and supersede operations. [src/router.rs:172] [src/router.rs:213] [src/router.rs:271] [measured] The standalone server opens one `Storage` and wraps it in `Arc<RwLock<Storage>>`. [src/main.rs:30] [src/main.rs:44] [measured] WAL append durability is ordered through a writer thread and synchronous append acknowledgements. [src/lib.rs:2035] [src/lib.rs:2311] [measured] In the inspected `Storage::open` path, `_lock_file` is initialized to `None`; that does not support the prompt's stronger claim that an OS file lock excludes a second writer process. [src/lib.rs:2193] [asserted] A spec-level document claims SWMR through `parking_lot::RwLock`, conceptual snapshot isolation through bitemporal edges, and future RCU snapshots. [docs/EXPANSION-SPEC--GENESIS-DB.md:45] [measured] A later ADR explicitly identifies the global REST `RwLock<Storage>`, lack of REST snapshots, and unbounded WAL as production findings, then says Wave 1 should replace the global RwLock and start an autonomic snapshot loop. [docs/ADR--GENESISDB-COMPETITIVE-SUPERIORITY.md:64] [docs/ADR--GENESISDB-COMPETITIVE-SUPERIORITY.md:203] [measured] `compact()` rebuilds the WAL from current live nodes and live edges only, which means old/retracted history is not preserved by compaction as a full historical MVCC log. [src/lib.rs:5478] [derived] SQLite WAL remains the consistency null hypothesis for this question because the local SQLite evidence says WAL provides snapshot isolation with concurrent readers and one writer. [docs/genesis-interview/evidence/r2-sqlite.md:56] [docs/genesis-interview/evidence/r2-sqlite.md:60]

**Falsifier / what would settle it:** [derived] A concurrency test should run N agents and, separately, N processes against the same database path/server while mixing reads, writes, supersedes, retractions, compaction, and `as_of`/historical queries. [src/router.rs:26] [src/lib.rs:5478] [derived] GenesisBlock passes the orchestrator snapshot claim only if readers get repeatable snapshots, no committed update is lost, historical queries remain valid after compaction, and second-process write behavior is explicit and safe. [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:53] [derived] It fails if the application must serialize externally, if readers observe mixed-version state, if compaction removes required history, or if two writers can open the same directory without a documented lock/merge protocol. [src/lib.rs:2193] [src/lib.rs:5478]

**Open questions:** [unknown] Which exact "PROTOCOL section 7" document is intended? The inspected evidence includes `RwLock` source and an ADR section 7 about replacing the global RwLock, but no literal `PROTOCOL section 7` file was identified. [docs/ADR--GENESISDB-COMPETITIVE-SUPERIORITY.md:203] [unknown] Is the product requirement "bitemporal query semantics" or true MVCC snapshot isolation for concurrent orchestrator reads/writes? [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:53]

## L-R3.4
**Verdict:** [derived] CRDT sync is a real engine-level asset, but "sleeper asset for on-device phone-cloud" is not yet proven; the tested core exists, while mobile transport, FFI sync exposure, and phone-cloud DoD remain incomplete or unknown. [src/lib.rs:4115] [docs/SPEC--MOBILE-SDK.md:361]

**Evidence:** [measured] `reconcile_state` verifies remote signatures, applies LWW-style node/edge/vector updates by clock, persists signed events, and advances the logical clock. [src/lib.rs:4115] [measured] `events_since` returns signed events newer than a supplied logical clock. [src/lib.rs:5393] [measured] Rust tests cover conflict resolution, peer signatures, remote higher-clock wins, logical-clock convergence, and forged-event rejection. [tests/crdt_sync_tests.rs:7] [tests/crdt_sync_tests.rs:146] [tests/crdt_sync_tests.rs:194] [measured] Anti-entropy tests cover `events_since` plus `reconcile_state`, idempotent delta application, and secondary vector sync/searchability. [tests/anti_entropy_tests.rs:1] [tests/anti_entropy_tests.rs:81] [tests/anti_entropy_tests.rs:134] [tests/anti_entropy_tests.rs:157] [measured] NAPI exposes `reconcile_state(events_json)` and `events_since(from_clock)`. [src/lib.rs:5948] [measured] REST routes include swarm and consensus routes, but no inspected REST route exposes `reconcile_state` or `events_since`. [src/router.rs:640] [measured] The mobile C FFI does not expose sync functions. [src/ffi.rs:107] [docs/SPEC--MOBILE-SDK.md:139] [asserted] The mobile spec says CRDT sync with desktop is existing or a Phase A toggle, but its Phase A DoD item for CRDT sync exchanges with desktop is unchecked and it flags mobile-network UDP risk with HTTP fallback. [docs/SPEC--MOBILE-SDK.md:361] [docs/SPEC--MOBILE-SDK.md:554] [docs/SPEC--MOBILE-SDK.md:611]

**Falsifier / what would settle it:** [derived] The sleeper-asset claim is confirmed by a device/cloud trial: phone makes offline edits, cloud/orchestrator makes concurrent edits, reconnect occurs over a mobile-safe transport, both converge through exported sync APIs, privacy policy sends only curated summaries to cloud, and conflicts are resolved or surfaced per policy. [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:32] [derived] The claim is refuted if sync requires raw DB upload, cannot run through mobile FFI/SDK, relies on blocked UDP without fallback, fails convergence under concurrent edits, or loses bitemporal history needed for audit. [src/ffi.rs:107] [docs/SPEC--MOBILE-SDK.md:611] [src/lib.rs:5478]

**Open questions:** [unknown] What mobile-safe transport is implemented today for CRDT exchange: HTTP push, background task, local network, or something else? [docs/SPEC--MOBILE-SDK.md:611] [unknown] What conflict policy should phone-cloud products use beyond LWW: user review, field merge, causal merge, or latest-wins only? [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:39] [unknown] What API surface will expose sync to iOS/Android if the current C FFI omits it? [src/ffi.rs:107]

## LYRA - verdict
[derived] Round 3 should update the thesis, not decide it: local model self-reports are weak hypothesis evidence; GenesisBlock's graph+G3+bitemporal+CRDT bundle may fit constrained orchestrators and on-device agents, but the current code proves less than the marketing shape. [docs/ROUND3-LOCAL-MODEL-FINDINGS.md:8] [measured] The strongest measured assets are core WAL ordering, tested CRDT reconciliation, NAPI sync exposure, and mobile-core/FFI build surfaces. [src/lib.rs:2035] [tests/crdt_sync_tests.rs:7] [src/lib.rs:5948] [Cargo.toml:34] [measured] The strongest measured gaps are REST global `RwLock<Storage>`, no proven MVCC snapshot isolation, no observed REST/mobile FFI sync surface, and compaction that keeps live state rather than full historical MVCC state. [src/router.rs:26] [src/router.rs:640] [src/ffi.rs:107] [src/lib.rs:5478] [derived] The next valid move is a falsification harness against SQLite/Redis/app-fusion baselines, plus a phone-cloud CRDT trial, before claiming the buyer is orchestrator plus on-device. [docs/BENCH-SPEC--HQL-MOAT-AND-EXPRESSIVENESS.md:85] [docs/SPEC--MOBILE-SDK.md:361]
